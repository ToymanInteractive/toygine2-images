name: Build Sega Mega Drive/Genesis toolchain

on:
  push:
    branches:
      - main
    paths:
      - Dockerfile.md
      - .github/workflows/build_md.yaml
  pull_request:
    branches:
      - main
    paths:
      - Dockerfile.md
      - .github/workflows/build_md.yaml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/_docker_build.yaml
    with:
      image: md
      # ClownMDSDK toolchain (incl. GCC) is built from source; arm64 under QEMU needs extra time.
      timeout: 240
